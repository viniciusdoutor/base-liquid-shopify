{
  "name": "base-liquid-shopify",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test:static": "node --test tests/static/*.test.mjs",
    "check": "shopify theme check --fail-level error"
  },
  "devDependencies": {
    "@shopify/cli": "^4.8.0"
  }
}
